{
  "name": "ai-agent-orchestrator-backend",
  "version": "1.0.0",
  "description": "Produktionsnaher API-Backend-Prototyp als Router zwischen Agenten, Qdrant und lokalen LLMs",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "dev": "tsx src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "test": "npm run build && node scripts/smoke-test.js",
    "smoke": "npm run build && node scripts/smoke-test.js",
    "benchmark": "npm run build && node dist/benchmark/benchmarkCli.js"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@fastify/helmet": "^13.0.0",
    "dotenv": "^16.4.7",
    "fastify": "^5.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
